import Card from '@components/ui/Card'

type Props = {}

export default function Example2({ }: Props) {
    return (
        <section className="container mx-auto max-w-[800px] my-10">
            <Card />
        </section>
    )
}